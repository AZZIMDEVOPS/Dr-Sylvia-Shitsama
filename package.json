{
  "name": "dr-sylvia-shitsama-regnl",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:cpanel": "astro build && powershell -Command \"if (Test-Path cpanel_build.zip) { Remove-Item cpanel_build.zip }; Compress-Archive -Path 'dist\\*', 'dist\\.htaccess' -DestinationPath 'cpanel_build.zip'\"",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.10",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.1.6",
    "gsap": "^3.15.0",
    "shiki": "^4.4.1",
    "tailwindcss": "^4.3.3",
    "typescript": "^6.0.3"
  }
}
